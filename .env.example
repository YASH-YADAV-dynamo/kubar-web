# Copy this file to .env and set the real credentials before running the app.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:password@34.131.183.158:5432/kubar"

# Accepted values: "true" (default, strict cert check), "allow-invalid" (TLS without cert validation), "false" (no TLS)
DATABASE_SSL=true

# Optional: tune the maximum number of pooled connections.
DATABASE_POOL_MAX=5
