---
const blogPosts = [
  {
    title: "How NavDhan Shrinks the MSME Credit Gap",
    excerpt: "Why lender-grade APIs and sector data are the key to unlocking the $300B MSME opportunity.",
    date: "October 2025",
    category: "Insights",
    url: "https://kubarlabs.substack.com/p/navdhan-credit-gap"
  },
  {
    title: "Building Credit Intelligence on ONDC",
    excerpt: "A look at embedded lending workflows across marketplaces, ERPs, and lender stacks.",
    date: "September 2025",
    category: "Technology",
    url: "https://kubarlabs.substack.com/p/credit-intelligence-ondc"
  }
];
---

<section id="blog" class="section blog">
  <div class="container">
    <div class="section-header">
      <div class="section-badge">Blog</div>
      <h2 class="section-title">Latest Updates</h2>
      <p class="section-description">
        News, insights, and stories about making business lending better.
      </p>
    </div>

    <div class="blog-grid">
      {blogPosts.map((post) => (
        <article class="blog-card">
          <div class="blog-category">{post.category}</div>
          <h3 class="blog-title">{post.title}</h3>
          <p class="blog-excerpt">{post.excerpt}</p>
          <div class="blog-meta">
            <span class="blog-date">{post.date}</span>
            <a class="blog-link" href={post.url} target="_blank" rel="noopener noreferrer">Read on Substack →</a>
          </div>
        </article>
      ))}
    </div>

    <div class="blog-cta">
      <div class="subscribe-card">
        <h3>Stay Updated</h3>
        <p>
          Get the latest news about NavDhan and updates on our launch. 
          We'll share insights about MSME lending and the future of business finance in India.
        </p>
        <a href="/blog" class="btn btn-primary">
          Read the Latest Posts
          <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
            <path d="M2.5 6.66667L10 11.6667L17.5 6.66667M3.33333 15H16.6667C17.5871 15 18.3333 14.2538 18.3333 13.3333V6.66667C18.3333 5.74619 17.5871 5 16.6667 5H3.33333C2.41286 5 1.66667 5.74619 1.66667 6.66667V13.3333C1.66667 14.2538 2.41286 15 3.33333 15Z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .blog {
    background: var(--color-surface);
  }

  .section-header {
    text-align: center;
    max-width: 800px;
    margin: 0 auto var(--spacing-2xl);
  }

  .section-badge {
    display: inline-block;
    padding: 0.375rem 0.875rem;
    background: var(--color-surface-elevated);
    border: 1px solid var(--color-border);
    color: var(--color-primary);
    border-radius: 6px;
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: var(--spacing-md);
  }

  .section-title {
    margin-bottom: var(--spacing-md);
    color: var(--color-text-primary);
  }

  .section-description {
    font-size: 1.25rem;
    line-height: 1.6;
    color: var(--color-text-secondary);
  }

  .blog-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--spacing-lg);
    margin-bottom: var(--spacing-2xl);
  }

  .blog-card {
    padding: var(--spacing-xl);
    background: var(--color-background);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    transition: all var(--transition-base);
    display: flex;
    flex-direction: column;
  }

  .blog-card:hover {
    border-color: rgba(74, 158, 255, 0.3);
    transform: translateY(-4px);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.3);
  }

  .blog-category {
    display: inline-block;
    padding: 0.375rem 0.75rem;
    background: rgba(74, 158, 255, 0.1);
    border: 1px solid rgba(74, 158, 255, 0.2);
    color: var(--color-primary);
    border-radius: 6px;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: var(--spacing-md);
    align-self: flex-start;
  }

  .blog-title {
    font-size: 1.375rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin-bottom: var(--spacing-sm);
    line-height: 1.3;
  }

  .blog-excerpt {
    font-size: 1rem;
    line-height: 1.6;
    color: var(--color-text-secondary);
    margin-bottom: var(--spacing-md);
    flex-grow: 1;
  }

  .blog-meta {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--spacing-md);
    padding-top: var(--spacing-sm);
    border-top: 1px solid var(--color-border);
  }

  .blog-date {
    font-size: 0.875rem;
    color: var(--color-text-tertiary);
  }

  .blog-link {
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--color-primary);
  }

  .blog-cta {
    margin-top: var(--spacing-xl);
  }

  .subscribe-card {
    max-width: 700px;
    margin: 0 auto;
    padding: var(--spacing-xl);
    background: linear-gradient(135deg, rgba(74, 158, 255, 0.1), rgba(74, 158, 255, 0.05));
    border: 1px solid rgba(74, 158, 255, 0.2);
    border-radius: 16px;
    text-align: center;
  }

  .subscribe-card h3 {
    font-size: 1.75rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin-bottom: var(--spacing-md);
  }

  .subscribe-card p {
    font-size: 1.125rem;
    line-height: 1.7;
    color: var(--color-text-secondary);
    margin-bottom: var(--spacing-lg);
  }

  @media (max-width: 768px) {
    .blog-grid {
      grid-template-columns: 1fr;
    }

    .section-description {
      font-size: 1.125rem;
    }

    .subscribe-card h3 {
      font-size: 1.5rem;
    }
  }
</style>
