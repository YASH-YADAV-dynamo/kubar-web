---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<BaseLayout title="Blog — Kubar Labs" description="Insights from Kubar Labs on MSME lending, ONDC, and credit intelligence.">
  <Header />
  <main class="blog-page">
    <section class="blog-hero">
      <div class="container">
  <h1>From the Kubar Labs Notebook</h1>
  <p>Sharing our insights from building India's first MSME-focused plug-and-play Embedded Business-Credit marketplace!</p>
      </div>
    </section>

    <section class="blog-embeds">
      <div class="container">
        <div class="embed-grid">
          <article class="embed-card">
            <header class="embed-header">
              <h2>Building India's B2B Credit Infrastructure</h2>
              <p>Part 1 of 6 · The infrastructure vs. application question for MSME lending.</p>
            </header>
            <div class="embed-wrapper">
              <div class="substack-post-embed"><p lang="en">Building India's B2B Credit Infrastructure: The Case for Population-Scale, Verifiable, and Cost-Efficient MSME Lending by Vaibhav Sharma</p><p>Part 1 of 6: The Infrastructure vs. Application Question</p><a data-post-link href="https://kubarlabs.substack.com/p/building-indias-b2b-credit-infrastructure">Read on Substack</a></div>
              <script async src="https://substack.com/embedjs/embed.js" charset="utf-8"></script>
            </div>
          </article>
          <article class="embed-card">
            <header class="embed-header">
              <h2>The Economics Are Broken (And Everyone Knows It)</h2>
              <p>Part 2 of 6 · Why traditional lending fails MSMEs and what metrics matter.</p>
            </header>
            <div class="embed-wrapper">
              <div class="substack-post-embed"><p lang="en">The Economics Are Broken (And Everyone Knows It But Nobody Has Fixed it ... Yet) by Vaibhav Sharma</p><p>Part 2 of 6: Why Traditional Lending Fails MSMEs and What Metrics Actually Matter</p><a data-post-link href="https://kubarlabs.substack.com/p/the-economics-are-broken-and-everyone">Read on Substack</a></div>
              <script async src="https://substack.com/embedjs/embed.js" charset="utf-8"></script>
            </div>
          </article>
        </div>
      </div>
    </section>
  </main>
  <Footer />
</BaseLayout>

<style>
  .blog-page {
    background: var(--color-background);
  }

  .blog-hero {
    padding: calc(var(--spacing-3xl) + 2rem) 0 var(--spacing-2xl);
    /* Decorative radial bubbles removed */
    background: var(--color-background);
  }

  .blog-hero h1 {
    font-size: clamp(2.5rem, 4vw, 3.25rem);
    margin-bottom: var(--spacing-md);
  }

  .blog-hero p {
    font-size: 1.2rem;
    color: var(--color-text-secondary);
  }

  .blog-embeds {
    padding: var(--spacing-3xl) 0 var(--spacing-3xl);
  }

  .embed-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: var(--spacing-2xl);
  }

  .embed-card {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-lg);
    padding: var(--spacing-xl);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 20px;
    min-height: 100%;
  }

  .embed-header h2 {
    font-size: 1.5rem;
    color: var(--color-text-primary);
    margin-bottom: var(--spacing-sm);
  }

  .embed-header p {
    color: var(--color-text-secondary);
    font-size: 0.95rem;
  }

  .embed-wrapper {
    border-radius: 14px;
    overflow: hidden;
    background: #ffffff;
    border: 1px solid var(--color-border);
    min-height: 420px;
  }

  .substack-post-embed {
    height: 100%;
  }

  .embed-card .btn {
    align-self: flex-start;
  }

  @media (max-width: 640px) {
    .embed-card {
      padding: var(--spacing-lg);
    }

    .embed-wrapper {
      min-height: 360px;
    }
  }
</style>
